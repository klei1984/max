name: Build Workflow
on:
   workflow_dispatch:
      inputs:
         build_type:
            description: 'Build Configuration to use (Debug, Release, RelWithDebInfo or MinSizeRel).'
            required: false
            default: 'Debug'

   push:
      branches:
      - master
      paths-ignore:
      - '**.md'
      - '**/doc'

   pull_request:
      types:
      - opened
      - synchronize
      - reopened
      paths-ignore:
      - '**.md'
      - '**/doc'

# CMake 4.4.1 regressed check_symbol_exists() to ignore CMAKE_REQUIRED_FLAGS, which breaks
# SDL3's _GNU_SOURCE feature checks. cmake/sdl3.cmake works around that for every platform,
# so this pin is only belt-and-braces against further 4.4.1 fallout and can be dropped.
env:
   CMAKE_APT_VERSION: 4.4.0-0kitware1ubuntu22.04.1

jobs:
   Linux-x64:
      name: Linux x64
      runs-on: ubuntu-latest
      container: ubuntu:24.04

      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt install -y file build-essential cmake=$CMAKE_APT_VERSION ninja-build ccache wget gettext p7zip-full libasound2-dev libpulse-dev libx11-dev libxext-dev libxcursor-dev libxrandr-dev libxfixes-dev libxi-dev libxss-dev libxtst-dev libxkbcommon-dev libwayland-dev wayland-protocols libdecor-0-dev libdbus-1-dev libgl-dev libegl-dev libudev-dev

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ~/.cache/ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            ccache -z
            cmake --preset linux-x64-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset linux-x64-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Test
         run: |
            ctest --preset linux-x64-${{steps.build_configuration.outputs.preset}}

      -  name: Package Source
         run: |
            cd build/linux-x64-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/Linux-x64
            cp *.tar.gz Artifacts/Linux-x64/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/linux-x64-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   Linux-x86:
      name: Linux x86
      runs-on: ubuntu-latest
      container: ubuntu:24.04

      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo dpkg --add-architecture i386
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt-get install -y file build-essential cmake=$CMAKE_APT_VERSION debmake dh-make gcc-multilib g++-multilib ninja-build:i386 ccache wget gettext p7zip-full libasound2-dev:i386 libpulse-dev:i386 libx11-dev:i386 libxext-dev:i386 libxcursor-dev:i386 libxrandr-dev:i386 libxfixes-dev:i386 libxi-dev:i386 libxss-dev:i386 libxtst-dev:i386 libxkbcommon-dev:i386 libwayland-dev:i386 wayland-protocols libdecor-0-dev:i386 libdbus-1-dev:i386 libgl-dev:i386 libegl-dev:i386 libudev-dev:i386

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ~/.cache/ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            ccache -z
            cmake --preset linux-x86-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset linux-x86-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Test
         run: |
            ctest --preset linux-x86-${{steps.build_configuration.outputs.preset}}

      -  name: Package Source
         run: |
            cd build/linux-x86-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/Linux-x86
            cp *.tar.gz Artifacts/Linux-x86/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/linux-x86-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   WindowsXP-x64:
      name: Windows XP x64
      runs-on: ubuntu-latest
      container: ubuntu:24.04

      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt-get install -y file build-essential binutils-mingw-w64-x86-64 g++-mingw-w64-x86-64 gcc-mingw-w64-x86-64 ninja-build cmake=$CMAKE_APT_VERSION ccache nsis gettext p7zip-full

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ~/.cache/ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            ccache -z
            cmake --preset cross-windowsxp-x64-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset cross-windowsxp-x64-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Package
         run: |
            cd build/cross-winxp-x64-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackConfig.cmake
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/WindowsXP-x64
            cp *-win64.exe Artifacts/WindowsXP-x64/
            cp *-win64.7z Artifacts/WindowsXP-x64/
            cp *-Source.7z Artifacts/WindowsXP-x64/
            7z a -mx9 -bd max-port-win64-symbols.7z max-port.debug
            cp max-port-win64-symbols.7z Artifacts/WindowsXP-x64/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/cross-winxp-x64-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   WindowsXP-x86:
      name: Windows XP x86
      runs-on: ubuntu-latest
      container: ubuntu:24.04

      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo dpkg --add-architecture i386
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt-get install -y file build-essential binutils-mingw-w64-i686 g++-mingw-w64-i686 gcc-mingw-w64-i686 ninja-build:i386 cmake=$CMAKE_APT_VERSION ccache nsis gettext p7zip-full

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ~/.cache/ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            ccache -z
            cmake --preset cross-windowsxp-x86-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset cross-windowsxp-x86-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Package
         run: |
            cd build/cross-winxp-x86-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackConfig.cmake
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/WindowsXP-x86
            cp *-win32.exe Artifacts/WindowsXP-x86/
            cp *-win32.7z Artifacts/WindowsXP-x86/
            cp *-Source.7z Artifacts/WindowsXP-x86/
            7z a -mx9 -bd max-port-win32-symbols.7z max-port.debug
            cp max-port-win32-symbols.7z Artifacts/WindowsXP-x86/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/cross-winxp-x86-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   Windows-x64:
      name: Windows x64
      runs-on: windows-2022

      defaults:
         run:
            shell: msys2 {0}

      steps:
      -  name: Install MSYS2
         id: msys2
         uses: msys2/setup-msys2@v2
         with:
            msystem: UCRT64
            update: false
            release: true
            cache: true

      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Setup Dependencies
         run: |
            pacman -S --noconfirm git mingw-w64-ucrt-x86_64-toolchain p7zip mingw-w64-ucrt-x86_64-cmake mingw-w64-ucrt-x86_64-ninja mingw-w64-ucrt-x86_64-nsis mingw-w64-ucrt-x86_64-gettext mingw-w64-ucrt-x86_64-ccache make

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ${{ runner.temp }}/.ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            MSYS2_ROOT: ${{steps.msys2.outputs.msys2-location}}
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            export CCACHE_DIR="$(cygpath -m '${{ runner.temp }}')/.ccache"
            echo "ccache dir: $CCACHE_DIR"
            ccache --show-config 2>/dev/null | grep -i cache_dir || true
            ccache -z
            cmake --preset msys2-ucrt64-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset msys2-ucrt64-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Test
         run: |
            ctest --preset msys2-ucrt64-${{steps.build_configuration.outputs.preset}}

      -  name: Package
         run: |
            cd build/ucrt64-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackConfig.cmake
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/Windows-x64
            cp *-win64.exe Artifacts/Windows-x64/
            cp *-win64.7z Artifacts/Windows-x64/
            cp *-Source.7z Artifacts/Windows-x64/
            7z a -mx9 -bd max-port-win64-symbols.7z max-port.debug
            cp max-port-win64-symbols.7z Artifacts/Windows-x64/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/ucrt64-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   Windows-x86:
      name: Windows x86
      runs-on: windows-2022

      defaults:
         run:
            shell: msys2 {0}

      steps:
      -  name: Install MSYS2
         id: msys2
         uses: msys2/setup-msys2@v2
         with:
            msystem: MINGW32
            update: false
            release: true
            cache: true

      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Setup Dependencies
         run: |
            pacman -S --noconfirm git mingw-w64-i686-toolchain p7zip mingw-w64-i686-cmake mingw-w64-i686-ninja mingw-w64-i686-nsis mingw-w64-i686-gettext mingw-w64-i686-ccache make

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Cache dependencies
         uses: actions/cache@v6
         with:
            path: dependencies
            key: ${{ runner.os }}-dependencies-${{ hashFiles('dependencies/dependencies.md5') }}
            restore-keys: |
               ${{ runner.os }}-dependencies-

      -  name: Cache ccache
         uses: actions/cache@v6
         with:
            path: ${{ runner.temp }}/.ccache
            key: ${{ runner.os }}-ccache-${{ github.job }}-${{ github.sha }}
            restore-keys: |
               ${{ runner.os }}-ccache-${{ github.job }}-

      -  name: Download dependencies
         continue-on-error: true
         run: |
            mkdir -p dependencies
            bash cmake/scripts/linux/get-dependencies cmake/versions.cmake

      -  name: Build
         env:
            MSYS2_ROOT: ${{steps.msys2.outputs.msys2-location}}
            CMAKE_C_COMPILER_LAUNCHER: ccache
            CMAKE_CXX_COMPILER_LAUNCHER: ccache
         run: |-
            export CCACHE_DIR="$(cygpath -m '${{ runner.temp }}')/.ccache"
            echo "ccache dir: $CCACHE_DIR"
            ccache --show-config 2>/dev/null | grep -i cache_dir || true
            ccache -z
            cmake --preset msys2-mingw32-${{steps.build_configuration.outputs.preset}}
            cmake --build --preset msys2-mingw32-${{steps.build_configuration.outputs.preset}}
            ccache -s

      -  name: Test
         run: |
            ctest --preset msys2-mingw32-${{steps.build_configuration.outputs.preset}}

      -  name: Package
         run: |
            cd build/mingw32-${{steps.build_configuration.outputs.preset}}
            cpack --config CPackConfig.cmake
            cpack --config CPackSourceConfig.cmake
            mkdir -p Artifacts/Windows-x86
            cp *-win32.exe Artifacts/Windows-x86/
            cp *-win32.7z Artifacts/Windows-x86/
            cp *-Source.7z Artifacts/Windows-x86/
            7z a -mx9 -bd max-port-win32-symbols.7z max-port.debug
            cp max-port-win32-symbols.7z Artifacts/Windows-x86/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: ${{github.job}}
            path: build/mingw32-${{steps.build_configuration.outputs.preset}}/Artifacts
            retention-days: 3

   Linux-deb-x64:
      name: Linux Debian x64
      runs-on: ubuntu-latest
      container: ubuntu:24.04
      needs: Linux-x64

      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{github.event.inputs.build_type}}
            BUILD_TYPE=${BUILD_TYPE:-"Debug"}
            echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT
            echo "preset=$(echo "${BUILD_TYPE}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright ||
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt install -y file build-essential cmake=$CMAKE_APT_VERSION ninja-build wget gettext p7zip-full libasound2-dev libpulse-dev libx11-dev libxext-dev libxcursor-dev libxrandr-dev libxfixes-dev libxi-dev libxss-dev libxtst-dev libxkbcommon-dev libwayland-dev wayland-protocols libdecor-0-dev libdbus-1-dev libgl-dev libegl-dev libudev-dev

      -  name: Download source package
         uses: actions/download-artifact@v8
         with:
            name: Linux-x64
            path: ${{steps.build_configuration.outputs.build_type}}/Sources/

      -  name: Package Debian
         run: |
            sudo apt install -y debmake debhelper dh-make
            cd ${{steps.build_configuration.outputs.build_type}}/Sources/Linux-x64
            for archive in max-port-*-Source.tar.gz; do
              dir="${archive%.tar.gz}"
              mkdir -p ../../../Debian/Linux-x64
              tar zxf "$archive" --directory=../../../Debian/Linux-x64
              cd ../../../Debian/Linux-x64/${dir}
              sh cmake/scripts/linux/get-dependencies cmake/versions.cmake
              debmake --tar --monoarch --invoke "debuild -us -uc"
              mkdir -p ../../../${{steps.build_configuration.outputs.build_type}}/Artifacts/Linux-x64
              cp ../max-port_*_amd64.deb ../../../${{steps.build_configuration.outputs.build_type}}/Artifacts/Linux-x64/
              cp ../max-port-dbgsym_*_amd64.ddeb ../../../${{steps.build_configuration.outputs.build_type}}/Artifacts/Linux-x64/
              cd -
            done

      -  name: Verify runtime dependencies
         run: |
            DEB_PACKAGE=$(ls ${{steps.build_configuration.outputs.build_type}}/Artifacts/Linux-x64/max-port_*_amd64.deb | head -1)
            sudo dpkg -i "${DEB_PACKAGE}" || sudo apt-get -f install -y

            MAXPORT_BINARY=$(dpkg -L max-port | grep -E '/max-port$' | grep -Ev '/(applications|icons)/' | head -1)
            MAXPORT_LIB_DIR=$(dirname "$(dpkg -L max-port | grep -E '\.so' | head -1)")

            if ! readelf -d "${MAXPORT_BINARY}" | grep -E 'RPATH|RUNPATH' | grep -q '\$ORIGIN'; then
               echo "::error::The executable does not carry the \$ORIGIN RPATH."
               exit 1
            fi

            ldd -r "${MAXPORT_BINARY}"

            if ldd -r "${MAXPORT_BINARY}" 2>&1 | grep -E "not found|undefined symbol"; then
               echo "::error::The Debian package is missing a runtime dependency of the executable."
               exit 1
            fi

            for LIBRARY in "${MAXPORT_LIB_DIR}"/*.so*; do
               if ldd "${LIBRARY}" 2>&1 | grep -E "not found"; then
                  echo "::error::${LIBRARY} is missing a runtime dependency."
                  exit 1
               fi
            done

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: Linux-deb-x64
            path: ${{steps.build_configuration.outputs.build_type}}/Artifacts
            retention-days: 3

   Linux-arch-x64:
      name: Linux Arch x64
      runs-on: ubuntu-latest
      container:
         image: archlinux:base-devel
         options: --user root
      needs: Linux-x64
      steps:
      -  name: Setup
         run: |
            useradd --create-home builder
            echo "builder ALL=(ALL) NOPASSWD: ALL" | sudo tee -a /etc/sudoers.d/builder
            pacman -Syu --noconfirm git

      -  name: Checkout
         uses: actions/checkout@v7
         with:
            fetch-depth: 0

      -  name: Build
         run: |
            sudo --login --set-home --user=builder /usr/bin/bash << EOFCMD
            git config --global --add safe.directory "$GITHUB_WORKSPACE"
            cat > PKGBUILD << 'EOF'
            # Copyright: 2026 M.A.X. Port Team
            # License: MIT

            # Maintainer: M.A.X. Port <maxport@maxport.org>
            pkgname="max-port"
            pkgver=0.8.0
            pkgrel=1
            pkgdesc="conquer and plunder-type turn-based sci-fi strategy game"
            arch=("\${CARCH}")
            url="https://github.com/klei1984/${GH_REPO}"
            license=("MIT")
            depends=("glibc" "gcc-libs")
            makedepends=("git" "cmake" "gcc" "ninja" "wget" "gettext" "p7zip" "libx11" "libxext" "libxcursor" "libxrandr" "libxfixes" "libxi" "libxss" "libxtst" "libxkbcommon" "wayland" "wayland-protocols" "libdecor" "alsa-lib" "libpulse" "dbus" "libglvnd")
            source=("\$pkgname::git+file://$GITHUB_WORKSPACE#commit=$GITHUB_SHA")
            provides=("\$pkgname")
            conflicts=("\$pkgname")
            sha256sums=("SKIP")

            pkgver() {
               cd "\$pkgname"
               _v=\$(git describe --long --tags --abbrev=8 --exclude="*[a-zA-Z][a-zA-Z]*" 2>/dev/null | sed -E "s/^[^0-9]*//;s/([^-]*-g)/r\1/;s/-/./g")
               [ -n "\$_v" ] || _v="\$pkgver.r\$(git rev-list --count HEAD).g\$(git rev-parse --short=8 HEAD)"
               printf '%s' "\$_v"
            }

            build() {
               cd "\${srcdir}/\${pkgname}"
                  DESTDIR=\$pkgdir cmake --no-warn-unused-cli -Wno-dev -G "Ninja" \
                  -DCMAKE_TOOLCHAIN_FILE=./cmake/toolchain-linux-\${CARCH}.cmake \
                  -DMAXPORT_PKGMAKE_BUILD=TRUE \
                  -DCMAKE_BUILD_TYPE=None \
                  -DBUILD_SHARED_LIBS=ON \
                  -DCMAKE_INSTALL_PREFIX=/usr \
                  -S . -B build
               cmake --build build --parallel
            }

            package() {
               cd "\${srcdir}/\${pkgname}"
               DESTDIR="\$pkgdir" cmake --install build
               install -Dm644 LICENSE -t "\${pkgdir}/usr/share/licenses/\${pkgname}"
               depends=("\${depends[@]/glibc/glibc>=\$(pacman -Q glibc | awk '{print \$2}' | cut -d- -f1 | cut -d+ -f1)}")
               depends=("\${depends[@]/gcc-libs/gcc-libs>=\$(pacman -Q gcc-libs | awk '{print \$2}' | cut -d- -f1 | cut -d+ -f1)}")
            }
            EOF
            makepkg --noconfirm --syncdeps --holdver
            mkdir -p Artifacts/Linux-x64
            cp max-port-*.pkg.tar.zst Artifacts/Linux-x64/
            EOFCMD

      -  name: Verify runtime dependencies
         run: |
            pacman -U --noconfirm /home/builder/Artifacts/Linux-x64/max-port-*.pkg.tar.zst

            if ! readelf -d /usr/bin/max-port | grep -E 'RPATH|RUNPATH' | grep -q '\$ORIGIN'; then
               echo "::error::The executable does not carry the \$ORIGIN RPATH."
               exit 1
            fi

            ldd -r /usr/bin/max-port

            if ldd -r /usr/bin/max-port 2>&1 | grep -E "not found|undefined symbol"; then
               echo "::error::The Arch package is missing a runtime dependency of the executable."
               exit 1
            fi

            pacman -S --noconfirm desktop-file-utils
            desktop-file-validate /usr/share/applications/max-port.desktop

            MAXPORT_EXEC=$(grep -E '^Exec=' /usr/share/applications/max-port.desktop | cut -d= -f2)

            if [ ! -x "${MAXPORT_EXEC}" ]; then
               echo "::error::The desktop entry Exec target '${MAXPORT_EXEC}' is not an executable file."
               exit 1
            fi

            if [ ! -f /usr/share/icons/hicolor/256x256/apps/max-port.png ]; then
               echo "::error::The desktop entry icon is not installed into the hicolor theme."
               exit 1
            fi

            for LIBRARY in /usr/lib/max-port/*.so*; do
               if ldd "${LIBRARY}" 2>&1 | grep -E "not found"; then
                  echo "::error::${LIBRARY} is missing a runtime dependency."
                  exit 1
               fi
            done

            echo "Runtime floor of the package:"
            readelf -V /usr/bin/max-port /usr/lib/max-port/*.so* 2>/dev/null \
               | grep -oE '(GLIBCXX|CXXABI|GLIBC)_[0-9.]+' | sort -uV \
               | awk -F_ '{floor[$1]=$0} END {for (family in floor) print "  " floor[family]}'

            pacman -Qi max-port | grep -E '^Depends'

            if ! pacman -Qi max-port | grep -E '^Depends' | grep -qE 'glibc>=[0-9]'; then
               echo "::error::The Arch package does not declare a versioned glibc dependency."
               exit 1
            fi

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: Linux-arch-x64
            path: /home/builder/Artifacts
            retention-days: 3

   Linux-flatpak-x64:
      name: Linux Flatpak x64
      runs-on: ubuntu-latest
      container:
         image: ubuntu:24.04
         options: --privileged
      needs: Linux-x64
      steps:
      -  name: Set build configuration
         id: build_configuration
         run: |
            BUILD_TYPE=${{ github.event.inputs.build_type }}
            echo "build_type=${BUILD_TYPE:-"Debug"}" >> $GITHUB_OUTPUT

      -  name: Cache APT packages
         uses: actions/cache@v6
         with:
            path: |
               /var/cache/apt/archives
               /var/lib/apt/lists
            key: ${{ runner.os }}-apt-${{ github.job }}-${{ hashFiles('**/.github/workflows/build.yml') }}
            restore-keys: |
               ${{ runner.os }}-apt-${{ github.job }}-
               ${{ runner.os }}-apt-

      -  name: Setup Dependencies
         run: |
            apt update -y && apt install -y sudo
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates gpg wget
            test -f /usr/share/doc/kitware-archive-keyring/copyright || \
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | sudo tee /etc/apt/sources.list.d/kitware.list >/dev/null
            sudo apt-get update -y
            test -f /usr/share/doc/kitware-archive-keyring/copyright || \
            sudo rm /usr/share/keyrings/kitware-archive-keyring.gpg
            sudo apt-get install kitware-archive-keyring
            sudo apt install -y file build-essential cmake=$CMAKE_APT_VERSION ninja-build wget gettext

      -  name: Download source package
         uses: actions/download-artifact@v8
         with:
            name: Linux-x64
            path: ${{steps.build_configuration.outputs.build_type}}/Sources/

      -  name: Package Flatpak
         run: |
            sudo apt install -y flatpak flatpak-builder
            flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
            flatpak install --assumeyes --noninteractive flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08
            tar zxf ${{steps.build_configuration.outputs.build_type}}/Sources/Linux-x64/max-port-*-Source.tar.gz --directory=${{steps.build_configuration.outputs.build_type}}/Sources/Linux-x64 --strip-components=1
            cd ${{steps.build_configuration.outputs.build_type}}/Sources/Linux-x64
            sh cmake/scripts/linux/get-dependencies cmake/versions.cmake
            cd ..
            flatpak-builder ${{steps.build_configuration.outputs.build_type}} --repo=repo --verbose --force-clean Linux-x64/flatpak/io.github.max-port.yaml
            flatpak build-bundle --verbose repo max-port.flatpak io.github.max-port
            mkdir -p ../Artifacts/Linux-x64
            cp max-port.flatpak ../Artifacts/Linux-x64/

      -  name: Upload Artifacts
         uses: actions/upload-artifact@v7
         with:
            name: Linux-flatpak-x64
            path: ${{steps.build_configuration.outputs.build_type}}/Artifacts
            retention-days: 3

   Nightly-Release:
      name: Nightly Release
      runs-on: ubuntu-latest
      if: github.event_name == 'push' && github.ref == 'refs/heads/master' && github.repository == 'klei1984/max'
      needs:
      - Linux-x64
      - Linux-x86
      - WindowsXP-x64
      - WindowsXP-x86
      - Windows-x64
      - Windows-x86
      - Linux-deb-x64
      - Linux-arch-x64
      - Linux-flatpak-x64

      permissions:
         contents: write

      concurrency:
         group: nightly-release
         cancel-in-progress: false

      env:
         NIGHTLY_TAG: nightly
         NIGHTLY_PREFIX: max-port-nightly
         GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
         GH_REPO: ${{github.repository}}

      steps:
      -  name: Download build artifacts
         uses: actions/download-artifact@v8
         with:
            path: artifacts

      -  name: Stage release assets
         run: |
            set -euo pipefail
            mkdir -p upload

            # publish <stable-name> <candidate-path>...
            # copies the first existing candidate under its permanent, version independent name
            publish() {
               local target="$1"
               shift
               local candidate
               for candidate in "$@"; do
                  if [ -f "${candidate}" ]; then
                     cp -- "${candidate}" "upload/${target}"
                     echo "${candidate} -> ${target}"
                     return 0
                  fi
               done
               echo "::error::no build artifact matched ${target} (looked for: $*)"
               return 1
            }

            publish "${NIGHTLY_PREFIX}-Windows_x86_64.exe"            artifacts/Windows-x64/Windows-x64/*-win64.exe
            publish "${NIGHTLY_PREFIX}-Windows_x86_64.7z"             artifacts/Windows-x64/Windows-x64/*-win64.7z
            publish "${NIGHTLY_PREFIX}-Windows_x86_64-symbols.7z"     artifacts/Windows-x64/Windows-x64/max-port-win64-symbols.7z

            publish "${NIGHTLY_PREFIX}-Windows_x86.exe"               artifacts/Windows-x86/Windows-x86/*-win32.exe
            publish "${NIGHTLY_PREFIX}-Windows_x86.7z"                artifacts/Windows-x86/Windows-x86/*-win32.7z
            publish "${NIGHTLY_PREFIX}-Windows_x86-symbols.7z"        artifacts/Windows-x86/Windows-x86/max-port-win32-symbols.7z

            publish "${NIGHTLY_PREFIX}-WindowsXP_x86_64.exe"          artifacts/WindowsXP-x64/WindowsXP-x64/*-win64.exe
            publish "${NIGHTLY_PREFIX}-WindowsXP_x86_64.7z"           artifacts/WindowsXP-x64/WindowsXP-x64/*-win64.7z
            publish "${NIGHTLY_PREFIX}-WindowsXP_x86_64-symbols.7z"   artifacts/WindowsXP-x64/WindowsXP-x64/max-port-win64-symbols.7z

            publish "${NIGHTLY_PREFIX}-WindowsXP_x86.exe"             artifacts/WindowsXP-x86/WindowsXP-x86/*-win32.exe
            publish "${NIGHTLY_PREFIX}-WindowsXP_x86.7z"              artifacts/WindowsXP-x86/WindowsXP-x86/*-win32.7z
            publish "${NIGHTLY_PREFIX}-WindowsXP_x86-symbols.7z"      artifacts/WindowsXP-x86/WindowsXP-x86/max-port-win32-symbols.7z

            publish "${NIGHTLY_PREFIX}-Linux_x86_64.deb"              artifacts/Linux-deb-x64/Linux-x64/max-port_*_amd64.deb
            publish "${NIGHTLY_PREFIX}-Linux_x86_64.ddeb"             artifacts/Linux-deb-x64/Linux-x64/max-port-dbgsym_*_amd64.ddeb
            publish "${NIGHTLY_PREFIX}-Linux_x86_64.flatpak"          artifacts/Linux-flatpak-x64/Linux-x64/max-port.flatpak
            publish "${NIGHTLY_PREFIX}-Linux_x86_64.pkg.tar.zst"      artifacts/Linux-arch-x64/Linux-x64/max-port-*.pkg.tar.zst

            publish "${NIGHTLY_PREFIX}-Source.tar.gz"                 artifacts/Linux-x64/Linux-x64/*-Source.tar.gz
            publish "${NIGHTLY_PREFIX}-Source.7z"                     artifacts/Windows-x64/Windows-x64/*-Source.7z

            ( cd upload && sha256sum -- * ) > "${NIGHTLY_PREFIX}-SHA256SUMS.txt"
            mv "${NIGHTLY_PREFIX}-SHA256SUMS.txt" upload/

            ls -l upload

      -  name: Create or refresh the nightly release
         run: |
            set -euo pipefail

            NOTES_FILE="$(mktemp)"
            cat > "${NOTES_FILE}" << EOF
            **Automated development build - not a formal release.**

            These artefacts are rebuilt from the tip of \`master\` on every successful build
            and the download links below never change, so they always serve the newest build.

            | | |
            | --- | --- |
            | Commit | [\`${GITHUB_SHA:0:8}\`](https://github.com/${GH_REPO}/commit/${GITHUB_SHA}) |
            | Built | $(date -u '+%Y-%m-%d %H:%M UTC') |
            | Configuration | Debug |
            | Workflow run | [#${GITHUB_RUN_NUMBER}](https://github.com/${GH_REPO}/actions/runs/${GITHUB_RUN_ID}) |

            Development builds are compiled without optimizations and with assertions and
            developer only tooling (scenario editor, debug menus, verbose logging) enabled.
            They run noticeably slower than the official releases and they may be unstable.
            Use the official [releases](https://github.com/${GH_REPO}/releases) for playing.

            \`${NIGHTLY_PREFIX}-SHA256SUMS.txt\` lists the checksums of every asset of this build.
            EOF

            if gh release view "${NIGHTLY_TAG}" > /dev/null 2>&1; then
               gh api --method PATCH "repos/${GH_REPO}/git/refs/tags/${NIGHTLY_TAG}" -f sha="${GITHUB_SHA}" -F force=true > /dev/null ||
               echo "::warning::could not move the ${NIGHTLY_TAG} tag to ${GITHUB_SHA}"
               gh release edit "${NIGHTLY_TAG}" --title "Latest Development Build" --notes-file "${NOTES_FILE}" --prerelease --draft=false
            else
               gh release create "${NIGHTLY_TAG}" --target "${GITHUB_SHA}" --title "Latest Development Build" --notes-file "${NOTES_FILE}" --prerelease
            fi

      -  name: Upload release assets
         run: |
            set -euo pipefail

            gh release upload "${NIGHTLY_TAG}" upload/* --clobber

            # drop assets left over from earlier builds that are no longer produced
            mapfile -t assets < <(gh release view "${NIGHTLY_TAG}" --json assets --jq '.assets[].name')
            for asset in "${assets[@]}"; do
               if [ ! -f "upload/${asset}" ]; then
                  echo "deleting stale asset ${asset}"
                  gh release delete-asset "${NIGHTLY_TAG}" "${asset}" --yes
               fi
            done
